{
  "name": "@mediatool-poc/hub",
  "version": "1.0.0",
  "description": "The hub part of the app",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "watch": "rimraf dist && tsc --watch",
    "build": "rimraf dist && tsc",
    "build:esbuild": "rimraf dist && tsc --declaration --emitDeclarationOnly && node ./build.js"
  },
  "dependencies": {
    "@handsontable/react": "^5.0.0",
    "@mediatool-poc/build-tools": "1.0.0",
    "@mediatool-poc/ui": "1.0.0",
    "esbuild": "^0.11.19",
    "react-dnd": "^14.0.2",
    "react-dnd-html5-backend": "^14.0.0"
  }
}
