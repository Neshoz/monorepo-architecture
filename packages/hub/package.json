{
  "name": "@mediatool-poc/hub",
  "version": "1.0.0",
  "description": "The hub part of the app",
  "main": "dist/index.js",
  "module": "src/index.tsx",
  "scripts": {
    "tsc:watch": "tsc --watch --noEmit && yarn build:watch",
    "build:watch": "yarn build-tools build:esbuild -f hub",
    "build": "rimraf dist && tsc"
  },
  "dependencies": {
    "@handsontable/react": "^5.0.0",
    "@mediatool-poc/ui": "1.0.0",
    "esbuild": "^0.11.19",
    "react-dnd": "^14.0.2",
    "react-dnd-html5-backend": "^14.0.0",
    "react-redux": "^7.2.4",
    "redux": "^4.1.0"
  },
  "devDependencies": {
    "tsc-watch": "^4.2.9"
  }
}
